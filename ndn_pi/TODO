Enforce localhost security with some default key '/localhost/operator' created in ndn-config

Expose insertIntoCapabilities, expand to include a function to call

Update ndn-config; need separate utility for configuring controller and
general nodes

Allow IotPolicyManager to add, remove rules when trust root is established 
